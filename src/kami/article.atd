@meta[
title = 深入 Rust 所有權系統：一份 @Doc 測試文章
author = WEDC
description = 用於驗證 Parser 與 Renderer 的完整節點覆蓋測試
keywords = rust,ownership,doc,test,ast
]

@h(1)[深入 Rust 所有權系統]

@p[
記憶體管理是系統程式語言的核心戰場。@bold[C] 把一切交給程式設計師，@bold[Java] 把一切交給 GC，而 @bold[Rust] 選擇了第三條路：@mark{yellow}[在編譯期靜態驗證記憶體安全]。這不是取捨，而是範式轉移。
]

@quote[
Ownership is Rust's most unique feature.
所有權是 Rust 最獨特的特性。
]

@hr

@h(2)[三條基本規則]

@list[
- 每個值都有一個擁有者（owner）
- 同一時間只能有一個擁有者
- 擁有者離開作用域時，值即被釋放
]

@important(核心觀察)[
所有權規則在@underline[編譯期]完成檢查，執行期@italic[零成本]。這是 Rust 與 GC 語言的根本差異。
]

@h(3)[移動語義示範]

@code(rust)[
fn main() {
    let s1 = String::from("hello");
    let s2 = s1;            // s1 的所有權移動到 s2
    // println!("{}", s1);  // 編譯錯誤：s1 已失效
    println!("{}", s2);
}
]

@warning(常見誤解)[
移動（move）不是複製。@del[以為賦值會自動深拷貝]是從其他語言帶來的直覺，在 Rust 中必須顯式呼叫 @raw[clone()]。
]

@caution[
使用 @raw[unsafe] 區塊會繞過借用檢查器，@mark[所有安全保證隨之失效]，請務必在 code review 中重點檢視。
]

@h(3)[借用與生命週期]

@p[
借用檢查器（borrow checker）確保引用永遠有效@fn[1]。規則很簡單：同一時間可以有多個不可變借用，@bold[或]一個可變借用，@mark{red}[但不能同時存在]@fn[2]。
]

@p[
@defn(1)[
See @link(https://doc.rust-lang.org/book/ch04-02-references-and-borrowing.html)[The Rust Book, Ch. 4.2].
]
]

@p[
@defn(2)[
這條規則在編譯期防止了資料競爭（data race），無需執行期鎖。
]
]

@hr

@h(2)[與其他語言比較]

@table[
    @cols[language,strategy,cost]

    @data[
        [C/C++,手動管理,懸空指標與洩漏風險]
        [Java/Go,垃圾回收,GC 暫停與記憶體開銷]
        [Rust,所有權系統,編譯期學習成本]
    ]
]

@tip(選型建議)[
如果專案對延遲敏感（遊戲引擎、嵌入式、高頻交易），所有權系統的前期學習成本會在後期加倍回收。
]

@h(2)[實務工作流程]

@tabs[
    @tab(安裝)[
        @p[
        使用 rustup 安裝工具鏈，完成後按 @kbd[Ctrl] + @kbd[`] 開啟終端機驗證版本。
        ]
    ]

    @tab(編譯)[
        @p[
        @code(text)[cargo build] 會執行完整的借用檢查，錯誤訊息包含修正建議。
        ]
    ]

    @tab(除錯)[
        @p[
        遇到 E0382（value moved）時，優先考慮借用而非 clone。
        ]
    ]
]

@h(3)[各平台安裝差異]

@tabs[
    @tab(Windows)[
        @p[
        透過官方安裝程式或 winget 安裝，需另外安裝 MSVC Build Tools 作為連結器。
        ]
    ]

    @tab(macOS)[
        @p[
        透過 rustup 或 Homebrew 安裝，需先安裝 Xcode Command Line Tools。
        ]
    ]

    @tab(Linux)[
        @p[
        透過發行版套件管理器或 rustup 安裝，通常已內建 gcc 可直接作為連結器。
        ]
    ]
]

@mermaid[
graph TD
A[宣告變數] --> B[取得所有權]
B --> C{離開作用域?}
C -->|是| D[自動呼叫 Drop]
C -->|否| E[繼續持有]
]

@img(
https://example.com/ownership-diagram.png,width=480,align=center
)[
所有權移轉流程示意圖
]

@hr

@h(2)[附錄：行內節點測試]

@p[
科學記號：E = mc@sup[2]，化學式：H@sub[2]O。@n
換行測試：第一行@n第二行。
]

@p[
連結推導測試：造訪@link(github.com/rust-lang/rust)[Rust 原始碼]，回報問題請寄@link(bugs@example.com)[回報信箱]，或撥打@link(+886912345678)[技術支援]。
]

@p[
逃逸測試：字面輸出 @@bold 不會被解析；整段跳過：@raw[這裡的 @mark[hello] 保持原樣]。@n
不成對括號跳脫測試：@raw[字面右括號 @] 與字面 @@@] 序列都能輸出]。@n
不成對左括號跳脫測試：@raw[缺少開頭方括號的字面右括號 @] 測試，以及缺少結尾方括號的字面左括號 @[ 測試]。
]

@details(巢狀嵌套測試)[
    @p[
    @bold[粗體外層包含@mark{blue,bordered}[藍色標記]與@italic[斜體加@underline[底線]]的組合。]
    ]
]

@card(Summary)[
所有權不是限制，而是契約。
編譯器是隊友，不是敵人。
]

@note[
本文件為 @Doc 節點覆蓋測試用途，內容涵蓋全部 Block 與 Inline 節點。
]
